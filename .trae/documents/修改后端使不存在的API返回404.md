## 项目变更摘要

### 1. FoxNAS (Qt客户端)
**修改文件：**
- `src/util/ApiRequest.cpp` - 修复 HTTPS 端口请求 Bug
- `src/util/ApiUrl.cpp` - 改进 URL 构建逻辑
- 多个翻译文件更新

**提交信息：**
```
feat(api): fix HTTPS request on port 443

- Fix bug where request URL was not updated after protocol switch
- Ensure QNetworkRequest uses the updated HTTPS URL
- Update translations for multiple languages
```

### 2. FoxNAServer (后端)
**修改文件：**
- `src/main/java/cn/programcx/foxnaserver/config/SecurityConfig.java` - 修改安全配置
- `src/main/java/cn/programcx/foxnaserver/controller/status/StatusController.java` - 更新状态接口
- `README.md`, `README-zh.md` - 文档更新

**新增文件：**
- Docker 相关配置文件

**提交信息：**
```
feat(security): update API access control and add Docker support

- Modify SecurityConfig to handle 404 responses correctly
- Update StatusController endpoint mappings
- Add Docker deployment configuration files
- Update project documentation
```

### 3. FoxNAS-Web (前端)
**修改文件：**
- `src/api/client.ts`, `src/api/services/file.ts` - API 客户端优化
- 多个视图组件更新
- 多语言翻译文件更新
- `vite.config.ts` - 构建配置

**新增文件：**
- `src/components/DownloadProgressDialog.vue`
- `src/stores/downloadProgress.ts`
- `LICENSE`, `README-zh.md`

**提交信息：**
```
feat(ui): add download progress and improve internationalization

- Add download progress dialog component
- Add download progress store for state management
- Update API client and file service
- Improve multi-language support for all views
- Add LICENSE and Chinese README
```

## 执行步骤

1. 对每个项目执行：
   - `git add -A` 添加所有变更
   - `git commit -m "feat(...): ..."` 提交
   - `git push` 推送到远程

请确认这个提交计划后，我将开始执行提交操作。